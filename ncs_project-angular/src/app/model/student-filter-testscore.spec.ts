import { StudentFilterTestscore } from './student-filter-testscore';

describe('StudentFilterTestscore', () => {
  it('should create an instance', () => {
    expect(new StudentFilterTestscore()).toBeTruthy();
  });
});
